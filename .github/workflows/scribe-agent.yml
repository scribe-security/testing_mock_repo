name: scribe agent

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      url:
        description: 'Scribe url'
        required: false
        default: 'https://api.scribesecurity.com'

jobs:
  scribe-agent:
    runs-on: ubuntu-latest
    container:
      image: scribesecuriy.jfrog.io/scribe-docker-local/agent:latest
      credentials:
        username: github
        password: ${{ secrets.scribe_artifactory_cred_id }}
    steps:
    - run: echo "Hello ${{ github.event.inputs.url }}!"
    - name: Checkout source
      uses: actions/checkout@v2
      with:
       fetch-depth: 0
    - run: agent --version
    - run: printenv
    - run: agent snapshot -vv -n github-push -u ${{ github.event.inputs.url }} -t ${{ secrets.scribe_backend_token_id }} -H github
